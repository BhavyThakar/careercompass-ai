# config.py

class Config:
    SECRET_KEY = "career-compass-secret-key"

    DB_CONFIG = {
        "host": "localhost",
        "user": "root",          # change if needed
        "password": "1234",  # change if needed
        "database": "career_compass_ai"
    }
